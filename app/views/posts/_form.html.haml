= simple_form_for @post do |f|

	%div.input
		= f.association :trends, collection: @five_random_trends, label_method: lambda { |t| "#{t.title} | #{t.description}" }, as: :check_boxes
		
		= f.input :image
		= f.input :title
		= f.input :link
		= f.input :description

		= f.button :submit
